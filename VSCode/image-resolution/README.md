# Image Resolution

打开或切换到图片预览时，插件会在 VS Code 右下角状态栏显示图片分辨率，例如 `1920 × 1080`。

支持 PNG、JPEG、GIF、WebP、BMP 和 SVG。状态栏只在当前活动 Tab 是本地图片时显示；切换到其他文件会自动隐藏。

## 本地安装

在本目录执行：

```bash
npx @vscode/vsce package
```

然后在 VS Code 扩展面板中选择“从 VSIX 安装”。
